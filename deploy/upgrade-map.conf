# Upgrade başlığı boşsa Connection'ı "upgrade" yapmak bazı istemcilerde
# bağlantıyı kırıyor; haritayla yalnızca gerçek websocket'te upgrade denir.
map $http_upgrade $connection_upgrade {
    default upgrade;
    ''      close;
}
